<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.gongu.mapper.UserMapper">

    <select id="idCheck" resultType="string">
        SELECT USER_ID
        FROM TBL_USERS
        WHERE USER_ID = #{userId}
    </select>


    <insert id="insert">
        INSERT INTO TBL_USERS (USER_NUMBER, USER_ID, USER_PASSWORD, USER_NAME, USER_NICKNAME, USER_EMAIL, USER_PHONE,
                               USER_MAJOR, USER_BIRTH, USER_LEVEL)
        VALUES (SEQ_USERS_NUMBER.NEXTVAL, #{userId}, #{userPassword}, #{userName}, #{userNickname},#{userEmail}, #{userPhone},#{userMajor},
                #{userBirth},#{userLevel})
    </insert>



    <select id="findByUserId" resultType="userDto">
        SELECT USER_NUMBER, USER_ID, USER_NAME, USER_NICKNAME, USER_EMAIL, USER_PHONE, USER_MAJOR,USER_BIRTH
        FROM TBL_USERS
        WHERE USER_ID = #{userId} AND USER_PASSWORD = #{userPassword}
    </select>







</mapper>